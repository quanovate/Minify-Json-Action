name: 'Minify-JSON'
author: 'Stefan Ensmann'
description: 'Minify JSON files'
branding:
  color: 'yellow'
  icon: 'minimize-2'
runs:
  using: 'composite'
  steps:
    - run: find . -name "*.json" | xargs -L1 python ${{ github.action_path }}/minify.py
      shell: bash
